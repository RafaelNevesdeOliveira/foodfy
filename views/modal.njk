{% extends "layout.njk" %}

{% block head %}

    <title>Foodfy - Receitas</title>

{% endblock %}

{% block content %}

    <main class="wrapper">

        <div class="images">
            {% for item in items %}
                <div class="image-home" id="{{item.id}}">
                    <img src="{{item.avatar_url}}" alt="Imagem de {{item.title}}">
                    <h5>{{item.title}}</h5>
                    <p>{{item.author}}</p>
                </div>
            {% endfor %}
        </div>

    </main>
    
    <script src="/scripts/scriptHome.js"></script>

{% endblock %}