{% extends "layout.njk" %}

{% block head %}

    <title>Foodfy - Home
    </title>

{% endblock %}

{% block content %}

    <div class="info">
        <div class="text">
            <h2>As melhores receitas</h2>
            <p>Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteiro.</p>
        </div>

        <div class="picture">
            <img src="/assets/chef.png" alt="chef">
        </div>
    </div>

    <main class="wrapper">

        <div class="images">
            {% for item in items %}
                <div class="image" id="{{item.id}}">
                    <img src="{{item.image_path}}" alt="Imagem de {{item.title}}">
                    <h5>{{item.title}}</h5>
                    <p>{{item.author}}</p>
                </div>
            {% endfor %}
        </div>

    </main>

{% endblock %}